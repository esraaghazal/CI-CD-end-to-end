apiVersion: v1
kind: Service
metadata:
  name: vproapp-service
spec:
  type: NodePort
  selector:
    app: vproapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: vproapp-port
      nodePort: 30594
